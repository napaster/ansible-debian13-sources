# {{ ansible_managed }}
# Источники apt переехали в {{ debian_sources_dir }}/*.sources (формат deb822).
# Этот файл оставлен пустым намеренно: строки из него apt читает наравне с
# .sources, и после апгрейда релиза здесь обычно остаются суффиксы старого.
